{
  "name": "@odysseus/gateway-policy",
  "version": "0.1.0",
  "type": "module",
  "description": "Gateway-side policy cache and local advisory evaluation — READ-ONLY, never authorizes",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "typecheck": "tsc --build --dry",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@odysseus/protocol": "workspace:*",
    "@odysseus/policy-engine": "workspace:*"
  },
  "devDependencies": {
    "@odysseus/protocol": "workspace:*",
    "@odysseus/policy-engine": "workspace:*",
    "vitest": "^2.0.0"
  }
}
